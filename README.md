# mnist-cnn-classifier
A production-grade PyTorch implementation of a Convolutional Neural Network (CNN) to classify handwritten digits using the MNIST dataset, featuring data augmentation and diagnostic performance tracking.
